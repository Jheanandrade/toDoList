const { resolve } = require('path')

module.exports = {
  config: resolve(__dirname, 'src', 'presentation', 'config', 'database.js' ),
  'migrations-path': resolve(__dirname, 'src','presentation', 'database', 'migrations'),
  'seeders-path': resolve(__dirname, 'src','presentation', 'database', 'seeds')
}
